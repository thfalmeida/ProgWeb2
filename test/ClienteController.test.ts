import {request } from 'node:http'
import { describe, it} from 'node:test'

describe('Teste para requisição de clientes na rota GET /cliente', function() {
    //Tentar retorno de http status para usuário autenticado e não autenticado
    //Testar login ou senha errado
})